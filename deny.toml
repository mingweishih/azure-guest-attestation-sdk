# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/

[advisories]
# Deny crates with known security vulnerabilities.
# Use `ignore = ["RUSTSEC-YYYY-NNNN"]` to temporarily suppress specific advisories.
ignore = [
    # rustls-pemfile is unmaintained but its functionality is now in
    # rustls-pki-types. Waiting for reqwest/hyper-rustls to drop the dep.
    "RUSTSEC-2025-0134",
]

[licenses]
# Allow these licenses for all dependencies.
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "BSL-1.0",
    "CDLA-Permissive-2.0",
]

[bans]
# Warn on multiple versions of the same crate (helps catch bloat).
multiple-versions = "warn"
wildcards = "allow"

[sources]
# Only allow crates from crates.io or specific git repos.
unknown-registry = "deny"
unknown-git = "allow"
allow-git = [
    "https://github.com/nicktrav/ms-tpm-20-ref",
]
